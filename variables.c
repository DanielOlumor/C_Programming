#include <stdio.h>
int main(){
    int number;
    float dec;
    printf("enter your inputs :");
    scanf("%d, %f", &number, &dec);

    printf("integer %d", number);
    printf("\nFloater %f", dec);
    return 0;
}